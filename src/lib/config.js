export const ATTR = Object.freeze({
  anim: 'anim',
  follow: 'follow',
  strength: 'strength',
  maxOffset: 'maxOffset',
  axis: 'axis'
});

export const CLASSNAMES = Object.freeze({
  readyHero: 'is-hero-ready'
});

export const DURATIONS = Object.freeze({
  default: 900,
  entranceDelay: 150
});

export const THRESHOLDS = Object.freeze({
  heroIntersect: 0.35
});

export const DATA_ATTRS = Object.freeze({
  hero: 'hero'
});
